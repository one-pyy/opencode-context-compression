# opencode-context-compression — Notepad Index

Project: /root/_/opencode/opencode-context-compression
Purpose: standalone OpenCode context compression plugin research and implementation.

---

[learnings/2026-04-02_initial-hook-shapes-and-session-transport-boundary] — Initial seam-debug run captured real `chat.params`, `messages.transform`, and `tool.execute.before` payload shapes; ordinary `session.prompt` / `prompt_async` remain unsafe default compaction transports because they traverse normal session prompt flow
[learnings/2026-04-03_mark-batch-freeze-lock-cleanup] — When batch dispatch acquires the file lock before SQLite batch persistence, any persistence failure must release the lock immediately or ordinary chat can wait on a batch that never existed
[learnings/2026-04-03_messages-transform-role-type-surface-vs-projected-artifacts] — The installed plugin type surface narrows transform message roles to user/assistant, so derived protected reminder artifacts must encode protection in projection state instead of assuming literal `system` role output
[learnings/2026-04-03_send-entry-wait-must-join-live-lock-with-batch-status] — Send-entry wait cannot treat an unlocked file as success because the runner deletes the lock after writing terminal batch state; success/failure must be resolved from the frozen batch row keyed by lock start time
[learnings/2026-04-03_nested-opencode-run-loading-proof-vs-direct-hook-fixtures] — Nested `opencode run` launches inside the Node test harness were unreliable for plugin-loading proof, so stable e2e coverage should initialize the plugin entry directly and exercise returned hooks against a temp project directory
[learnings/2026-04-03_mark-tool-exposure-boundary] — This repo contains internal mark persistence and batch-freeze code, but does not expose a repo-local public mark tool; legacy `dcp_mark` names only appear in runtime gate policy
[learnings/2026-04-03_old-dcp-fork-still-active-through-tools-runtime-client] — Even without the old fork in the plugin list, current DCP tools still import opencode-dcp-fork runtime modules and use enabled config/dcp-runtime.json, so legacy DCP behavior remains live through tool execution
[learnings/2026-04-03_chat-params-cutover-gap-is-observer-replacement-not-missing-hook] — `src/index.ts` already returns a `chat.params` hook via noop observation spread, so scheduler cutover must replace an existing observer seam rather than add a missing hook
[learnings/2026-04-03_scheduler-cutover-defaults-and-config-compat] — Task 4 scheduler cutover must keep `chat.params` non-blocking by default and add new runtime-config fields with deterministic backward-compatible defaults
[learnings/2026-04-03_task7-e2e-requires-injected-safe-transport] — Task 7 end-to-end proof should load the real plugin entry and scheduler seam, but must inject a plugin-safe transport because the default scheduler transport intentionally reports unavailable until a repo-owned executor exists
[decisions/2026-04-03_final-repo-owned-operator-contract] — Final operator contract is repo-owned: public marking uses only `compression_mark`, canonical config and prompt live in this repo, and full keep/delete proof comes from repo-owned tests rather than legacy host tools
[tutorials/2026-04-03_repo-owned-operator-and-live-verification-workflow] — For repo-owned operator and live verification tasks: update canonical docs, preserve the truth boundary around real-session proof, and keep the docs and notepad contract enforced by the cutover audit test
[problems/2026-04-04_design-doc-visible-id-contract-still-conflicts-with-later-decisions] — DESIGN.md still uses role-prefixed visible-id examples even though later decisions require single-exit rendering and three-state model-visible prefixes
[decisions/2026-04-04_design-doc-reminder-cadence-and-allowdelete-contract] — DESIGN.md now treats repeated reminders as explicit token-based cadence and `allowDelete=true` as a fully supported delete-permission path, not a future-only or fail-fast placeholder
[decisions/2026-04-04_tool-only-assistant-shell-and-per-tool-msg-id-rendering] — In tool-only turns, render one minimal assistant shell containing only the visible id before the batch of tool calls; each tool result must carry its own msg id at the front, with array outputs inserting the id as the first text item
[decisions/2026-04-04_assistant-visible-id-prefix-when-body-exists] — If assistant text already exists, prepend the assistant visible id directly to that text; only synthesize an id-only assistant shell when the turn has tool calls but no assistant text, and always prepend each tool result msg id at the front
[tutorials/2026-04-04_design-doc-change-log-usage] — For DESIGN.md-driven implementation tasks: read the repo-root design change log first to distinguish target design changes from behaviors already observed in the current codebase
[problems/2026-04-04_design-contract-audit-shows-code-still-on-legacy-route-reminder-and-rendering-contract] — Repo audit shows current code still implements legacy route-centric config/reminder/projection contracts while DESIGN.md now targets allowDelete, token-cadence reminders, four reminder prompts, and specialized assistant/tool rendering
[problems/2026-04-06_design-md-conflict-audit-for-mode-allowdelete-and-replay-contract] — DESIGN.md now contains explicit tensions between older allowDelete-persistence language and later mode/admission/replay rules, so implementation work must quote the conflicting text instead of silently collapsing it
