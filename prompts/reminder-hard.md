Hard reminder: compact older compressible context now unless it must remain verbatim.

Compressible content snapshot:
{{compressible_content}}

Current compaction target:
{{compaction_target}}

Preserved fields and protected context:
{{preserved_fields}}
