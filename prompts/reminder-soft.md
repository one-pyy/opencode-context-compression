Soft reminder: consider compacting older compressible context when it is no longer needed verbatim.

Compressible content snapshot:
{{compressible_content}}

Current compaction target:
{{compaction_target}}

Preserved fields and protected context:
{{preserved_fields}}
