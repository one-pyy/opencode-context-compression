**Immediately** use `compression_mark` to compress context no longer needed. You can store essential parts as intermediate result documents in `.sisyphus/tmp` for compression. Prioritize content with high compression ratios (i.e., a low ratio of intermediate result length to original length). You may mark multiple non-contiguous context blocks in a single response.