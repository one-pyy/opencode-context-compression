**Immediately** use `compression_mark` to compress context no longer needed. Before marking, check for unhandled leftovers from the previous task and avoid ranges that were already marked. Prioritize `compressible` messages: they are the highest-volume context most likely to benefit from compression. Store essential parts as intermediate result documents in `.sisyphus/tmp` for compression. Prioritize content with high compression ratios (i.e., a low ratio of intermediate result length to original length). You may mark multiple non-contiguous context blocks in a single response.
