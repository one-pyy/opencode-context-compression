**Immediately** reduce completed context. First use `compression_inspect` from the earliest candidate visible message to the current newest visible message to list compressible messages across the full span and check for overlooked completed content. This inspection is only an aid: do not compress or delete every listed message by default; mark only ranges whose work is complete, no longer needed verbatim, and not already covered by a successful mark. Use `compression_mark` for those ranges, and you may also mark previously compressed context for deletion. Prioritize high-volume `compressible` messages and high compression-ratio content. Store essential parts as intermediate result documents in `.sisyphus/tmp` before marking when needed. You may mark multiple non-contiguous context blocks in a single response. After marking, immediately continue the task without stopping to wait.
