/**
 * TODO(new-project): rebuild runtime-config around the new DESIGN-driven runtime.
 *
 * Direction:
 * - preserve the repo-owned config contract that still matches DESIGN.md
 * - reload compaction/reminder prompt assets explicitly
 * - remove config fields that only served stripped legacy runtime paths
 * - keep env override precedence explicit and fail-fast
 */
export {};
