/**
 * TODO(new-project): rewrite visible-sequence from DESIGN.md.
 *
 * Direction:
 * - keep bare visible ids plus single-exit rendering
 * - remove legacy role-prefix compatibility normalization
 * - derive compact block ids explicitly from min source sequence
 * - preserve monotonic sequence semantics without reviving old helpers
 */
export {};
