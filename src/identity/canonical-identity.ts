/**
 * TODO(new-project): rewrite canonical-identity helpers from DESIGN.md.
 *
 * Direction:
 * - keep message.info.id as the canonical host identifier unless DESIGN changes
 * - keep bare prompt-visible metadata read/write helpers
 * - reshape APIs for the new replay/projection flow instead of the legacy chain
 */
export {};
