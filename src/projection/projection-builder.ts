/**
 * TODO(new-project): rewrite projection-builder from DESIGN.md.
 *
 * Direction:
 * - build prompt-visible output from canonical history + history-first replay + result-group lookup
 * - remove legacy single-replacement fallback and first-item result-group assumptions
 * - render full atomic result groups
 * - render delete results as design-defined minimal delete notices
 * - drop all dependencies on persisted mark/source snapshot truth
 */
export {};
