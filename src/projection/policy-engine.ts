/**
 * TODO(new-project): rewrite policy-engine from DESIGN.md.
 *
 * Direction:
 * - classify system and short user messages as protected
 * - classify long user / assistant / tool as compressible
 * - attach only the visible identity data needed by the new projection pipeline
 * - avoid restoring legacy store-coupled policy assembly
 */
export {};
