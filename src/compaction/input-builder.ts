/**
 * TODO(new-project): rewrite compaction input builder from DESIGN.md.
 *
 * Direction:
 * - build compaction input from the current replay-derived source span
 * - stop treating source snapshots as long-lived truth objects
 * - preserve placeholder/opaque-block rules only if still required by the new design path
 */
export {};
