/**
 * TODO(new-project): rewrite compaction output validation from DESIGN.md.
 *
 * Direction:
 * - validate compact/delete outputs against the new result-group contract
 * - keep delete notice validation aligned with the new projection contract
 * - remove validation paths that only existed for legacy replacement records
 */
export {};
